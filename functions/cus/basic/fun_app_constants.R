# shared app-wide constants
# unified debounce delays (ms): numeric/count inputs vs sliders
DEBOUNCE_NUM    <- 400
DEBOUNCE_SLIDER <- 250

# maximum number of endpoints supported (matches pre-allocated vectors of length 10
# in initial_reactiveValues.R and the max of the endpoint-count numericInputs)
MAX_ENDPOINT <- 10
